# 打小抄

``` python
intervals.sort(key= lambda x: x[0])


# 堆的用法
import heapq
free_rooms = []
heapq.heappush(free_rooms, intervals[0][1])
heapq.heappop(free_rooms)


# 字符串相关
sorted("acb")
# ['a', 'b', 'c']
ord(c)

# 数值相关
-float('inf'), float('inf')


```


紧急整理
- 用栈实现中序遍历